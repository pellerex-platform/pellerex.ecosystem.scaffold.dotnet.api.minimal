# Pellerex-owned paths. Any PR touching these waits on review from the
# pellerex-guardians team in this tenant Org — a team populated with a
# Pellerex operator so the deploy layer stays consistent with the platform.
# The CODEOWNERS file protects itself so this guard can't be removed in a
# single PR. Tenants remain free to change everything else without approval
# from Pellerex.
/infrastructure/     <pellerex-guardians-owner>
/.github/CODEOWNERS  <pellerex-guardians-owner>
